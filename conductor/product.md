# Product Guide

## Product Name
trikeshed

## Product Summary
trikeshed is the Kotlin core for immutable series/cursor algebra and expression-based compute paths used by adjacent trading systems.

## Current Conductor Focus
Keep TrikeShed as the working Kotlin exemplar: land the commonMain manifold semantic layer, repair the open JSON runtime/vectorization tracks, preserve the real `Join` / `Series` / `Cursor` lineage, and do not let cppfort set local product priority while it remains an unstable adjacent experiment.

## Scope Clarifications
- `hrm` logic is located in the sibling repository `../moneyfan`.
- Input/output concerns are handled by `../curly-succotash`.
- TrikeShed is the source of truth for Series/Cursor/Grammar and the working Kotlin exemplar; leaf nodes live in consuming apps.
- `freqtrade` is being retired; beneficial logic (indicators, strategy hooks) is being extracted.
