# Ant globs excluded from the Kotlin/Native compiles and compileCommonMainKotlinMetadata ONLY.
# The JVM build is untouched. Delete a line once the file compiles on those targets = ratchet.
# java.util.TreeMap in commonMain plus flywheel-patched code that only resolves on jvm
**/parse/json/Codec.kt
# androidx Compose UI in commonMain; Compose is not a dependency of any target here
**/ui/palette/**
# expect declarations with jvm/linux-only actuals (mirrors the js ratchet's reasoning)
**/userspace/nio/ebpf/ExpectRunNative.kt
**/userspace/nio/ebpf/Probe.kt
**/userspace/nio/ebpf/codec/**
**/userspace/nio/ebpf/engine/**
**/userspace/nio/ebpf/io/**
**/userspace/nio/ebpf/probe/**
**/userspace/nio/ebpf/types/**
**/userspace/nio/ebpf/verifier/**
**/btrfs/BtrfsImageIo.kt
**/btrfs/NioBtrfsGraalBlobStore.kt
**/btrfs/NioBtrfsGraalBlobStoreTest.kt
**/org/trikeshed/oroboros/OroborosDaemon.kt
# java.util.concurrent.ConcurrentHashMap; needs a real Mutex/atomic-map rewrite
# (lookup() must stay a synchronous "pure map read" per its own doc comment,
# so this isn't a mechanical swap) -- see GH #768. Already on the JS list for the
# same reason; native inherited the break when the file landed and never got the line.
**/couch/IncrementalViewRegistry.kt
# jvm-only test debt for native test compiles
**/userspace/nio/process/ProcessWorkerContractTest.kt
**/forge/window/NativeForgeWindowManagerTest.kt
