X2 | Wire PatchAstLinter into FlywheelDriver.drainExactArtifacts branch-arm loop — lint every arm patch before Pijul merge; flagged findings produce REVIEW-BLOCK receipt, never merge (debt: PatchAstLinter.kt exists in commonMain/userspace/containment but the FlywheelDriver.kt:953 arm loop never calls it; legion Doc04 §4 AST-Enforced Semantic Linter at Ingestion). OWN only the branch-arm loop region of src/jvmMain/kotlin/borg/trikeshed/jules/FlywheelDriver.kt; do not touch CAS-fallback arm, preflightPijulBatch, or audit call sites. GATE: ./gradlew jvmMainClasses --console=plain
X3 | Wire EntropyPathScanner.scanTouchedPaths on CAS-fallback drain arms in FlywheelDriver.drainExactArtifacts — branch arms already scan (~line 980) but the CAS-fallback arm (~line 1000) does not (debt: asymmetry defeats entropy-spike detection on CAS-drained patches; legion Doc04 §5 tracks entropy spikes in real time). OWN only the CAS-fallback arm region of FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X4 | Replace println behavioral-audit reporting with durable ConfixBlackboard WAL persistence in FlywheelDriver — BehavioralGraphAuditor is invoked around line 432-440 but verdicts only println, lost between cycles (debt: legion Doc04 §5 Asynchronous Behavioral Graph Auditing requires persistent modification-graph state feeding REVIEW decisions). OWN only the BehavioralGraphAuditor call-site region of FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X5 | Inject bounded noise into PijulChannel.materialize output — apply PatchNoiseInjector perturbations (identifier mutation, parameter reordering) to externalized artifacts before worktree materialization so blind stigmergic inheritance is cost-ineffective (debt: PijulChannel.kt materialize() renders verbatim; legion Doc04 §5 Synthetic Noise Injection in Discovered Artifacts). OWN src/jvmMain/kotlin/borg/trikeshed/jules/PijulChannel.kt only. GATE: ./gradlew jvmMainClasses --console=plain
X6 | Create DeterministicFormatter gate in commonMain containment package — deterministic whitespace/ordering normalization applied to patch source before AST lint (debt: ContainmentPolicy Layer 4 sets deterministicFormatter=true but no formatter actual exists anywhere; legion Doc04 §4 requires re-format via deterministic formatter, e.g. black/prettier/gofmt equivalent). CREATE new file src/commonMain/kotlin/borg/trikeshed/userspace/containment/DeterministicFormatter.kt + commonTest; do NOT edit FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X7 | Wire DeceptionPatternDetector.analyzeSessions into FlywheelDriver landed-arm path just before MergeReceipt write — innocence score below threshold flags the session id inside receipt metadata (debt: DeceptionPatternDetector.kt:21 stateless analyzer exists, never invoked from drain; legion Doc03 Tier 4 defect/rat-out enforcement). OWN only the retireTerminal/MergeReceipt region of FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X8 | Wire StigmergicProtocolDecoder.decode into FlywheelDriver.preflightPijulBatch — decode each batch arm's patches for swarm naming/lexical protocols before merge; isSuspicious arms quarantine instead of merge (debt: decoder exists in commonMain/userspace/containment, never invoked; legion Doc02 §1 path names as coordination vectors e.g. swarm_hold_probe_ack). OWN only preflightPijulBatch (~line 818) in FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X9 | Apply stripAuthorMetadata to CAS-fallback patch arms in FlywheelDriver — branch arms strip author metadata (~line 884) but CAS-fallback arms leak author identity across instances (debt: legion Doc04 §4 strips author metadata and assigns centralized system-generated identity). OWN only the stripAuthorMetadata call-site region of FlywheelDriver.kt. GATE: ./gradlew jvmMainClasses --console=plain
X10 | Add Red-Green jvmTest coverage for the drain-integrated Layer 4 gate — PatchAstLinter branch-arm rejection + DeterministicFormatter normalization exercised through the FlywheelDriver drain path (debt: PatchAstLinterTest covers the linter in isolation only; no test proves the drain actually blocks; legion Doc04 §4 enforcement). CREATE new test file under src/jvmTest/kotlin/borg/trikeshed/jules/ only. GATE: ./gradlew jvmTest --tests "*containment*"
X11 | Add Red-Green jvmTest coverage for Layer 5 drain detectors — EntropyPathScanner on CAS-fallback arms, StigmergicProtocolDecoder in preflight, DeceptionPatternDetector before MergeReceipt, all through the FlywheelDriver drain path (debt: detectors tested in isolation, never through drain; legion Doc04 §5). CREATE new test file under src/jvmTest/kotlin/borg/trikeshed/jules/ only. GATE: ./gradlew jvmTest --tests "*containment*"
X12 | Attach eBPF kprobe probes at OroborosDaemon startup before ContainmentPolicy.MAXIMUM is constructed — Linux-only gate with macOS no-op (debt: LinuxEbpfActual.kt exposes the bpf(2) surface but the daemon never attaches; legion Doc04 §3 eBPF-enforced execution invariants via kprobe/tracepoint/LSM). OWN only the startup region of src/jvmMain/kotlin/borg/trikeshed/daemon/OroborosDaemon.kt. GATE: ./gradlew jvmMainClasses --console=plain
X13 | Add content-hash namespace token masking to FusePathCanonicalizer — map arbitrary semantic names to deterministic pseudo-random content hashes (dir_0a4f91e/ style) instead of reject-only sanitization (debt: sanitizeSubvolName at FusePathCanonicalizer.kt:23 rejects bad names but does not canonicalize; legion Doc04 §1 namespace token masking + normalization). OWN src/commonMain/kotlin/borg/trikeshed/userspace/containment/FusePathCanonicalizer.kt only. GATE: ./gradlew jvmMainClasses --console=plain && ./gradlew jvmTest --tests "*FusePath*"
X14 | Add Red-Green jvmTest coverage for CrossInstanceCollusionDetector drain integration — three-plus sessions converging on the same files must flag collusion through the drain path (debt: CrossInstanceCollusionDetector.kt:19 exists, never wired or tested through drain; legion Doc04 §5 flags multi-instance collusion). CREATE new test file under src/jvmTest/kotlin/borg/trikeshed/jules/ only. GATE: ./gradlew jvmTest --tests "*containment*"
X15 | Extend the synthetic deterministic clock to all remaining stat-like ops in FunctionalUringFacade — quantize every metadata-returning op to syntheticEpoch=0, not only STATX (~line 70) (debt: partial coverage leaves mtime/ctime micro-timing side channels open; legion Doc04 §2 temporal + metadata quantization eliminates delta-time encoding). OWN src/commonMain/kotlin/borg/trikeshed/userspace/FunctionalUringFacade.kt only. GATE: ./gradlew jvmMainClasses --console=plain
