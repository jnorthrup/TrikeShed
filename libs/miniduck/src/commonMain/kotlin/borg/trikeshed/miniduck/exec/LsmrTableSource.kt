package borg.trikeshed.miniduck.exec

// LSMR TableSource is JVM-only; keep no common expect/actual so JS/wasm builds don't require a platform implementation.
// If a platform-specific implementation is needed, add it under the appropriate source set.

