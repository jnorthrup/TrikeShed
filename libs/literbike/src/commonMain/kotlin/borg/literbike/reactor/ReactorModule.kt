/**
 * Port of /Users/jim/work/literbike/src/reactor/mod.rs
 *
 * Reactor foundation modules (portable baseline + compatibility stub).
 *
 * All reactor types are declared in this package. This file serves as the
 * module root, mirroring the Rust `mod.rs` re-exports.
 */
@file:Suppress("unused")

package borg.literbike.reactor

// Primary types mirroring the Rust `pub use` declarations:
//   pub use reactor::{Reactor, ReactorTickResult};
//   pub use simple_reactor::SimpleReactor;
//   pub use userspace_selector::UserspaceSelector;

// These are already declared as top-level types in this package.
// No typealiases needed since all files share the same package.
