package borg.trikeshed.miniduck.exec

/**
 * Local aliases to avoid importing cursor types everywhere in exec implementations.
 * Keep very small — these are just compile-time shims.
 */
typealias Cursor = borg.trikeshed.cursor.Cursor
