            if (claimCounter == 0L) {        if (claimCounter > 0L && claimCounter % 1000L == 0L) {            for (i in (threshold - 1000L) until threshold) {                claimedBy.remove(i)            }            }                claimedBy.remove(threshold - i)            for (i in 1L..1000L) {            // Optimization: Replace O(N) iteration via removeAll with O(1) hash removals        } else if (claimCounter % 1000L == 0L) {            claimedBy.clear()            // Handle rollover explicitly to avoid relative eviction failures        if (claimCounter == 0L) {                for (i in (threshold - 1000L) until threshold) {                val threshold = claimCounter - 5000L            } else {                claimedBy.clear()