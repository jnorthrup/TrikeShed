/**
 * TrikeShed Platform — Userspace Network
 *
 * Ported from literbike `src/userspace/network/`. Network adapters, channels,
 * and protocol implementations.
 *
 * - adapters: Network adapter implementations
 * - channels: Network channel abstractions
 * - protocols: Protocol implementations
 */
package borg.trikeshed.platform.network
