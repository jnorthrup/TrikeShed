package borg.trikeshed.platform.network

/**
 * Network abstractions and protocol adapters
 *
 * This module provides unified network protocol handling with adapters
 * for HTTP, QUIC, SSH, and other protocols.
 */

// Re-exported from submodules:
// - NetworkAdapter, AdapterType from adapters.kt
// - Protocol, ProtocolDetector from protocols.kt
// - Channel, ChannelProvider from channels.kt
