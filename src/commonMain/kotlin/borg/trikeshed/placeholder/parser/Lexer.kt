package borg.trikeshed.placeholder.parser

/**
 * hierarchical Lexer: a bifurcating lexer graph model for parsing InputSegments.
 *
 * The initial and resulting state of InputSegment provides a context with blackboard state and also a [LexerMode] that
 * defines forward iterator behaviors and the next FSM state to transition to.
 *
 */

class Lexer {

}